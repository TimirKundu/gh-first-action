name: First Workflow
on: workflow_dispatch # Triggers manually
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greetings
        run: echo "Hello World"
      - name: Print Goodbye
        run: echo "Done! Bye"
        
